---
name: create
description: >-
  Сборка прототипа / приложения / экрана / страницы на Gravity UI (Гравити) «как принято» — на
  настоящих компонентах (@gravity-ui/uikit, navigation, table, page-constructor, icons,
  dynamic-forms), с правильным выбором библиотеки, app-shell-обвязкой, закреплёнными версиями и
  опциональным per-service профилем. Используй, когда нужно сгенерировать React+TypeScript UI на
  Gravity UI, собрать лендинг / дашборд / форму / настройки / список / таблицу на Гравити, или когда
  в целевом проекте лежит service-profile.json.
---

# gravity-build

Собирай UI на **настоящих** компонентах Gravity UI «как принято», а не по памяти. Источник правды про
Гравити — бандл-registry рядом с этим скиллом; per-service вкус — опциональный профиль из целевого
проекта.

## Когда применять

- Запрос собрать / сверстать прототип, приложение, экран, страницу, компонент на **Gravity UI /
  Гравити** (лендинг, дашборд, форма, настройки, список, таблица…).
- Любая сборка React + TypeScript, где дизайн-система — Gravity UI.

## Порядок работы

1. **Открой knowledge-registry и следуй ему.** Entry-point — `registry/registry.json` (роутер, рядом с
   этим `SKILL.md`); прочти его первым и работай строго по полю `workflow`: scaffold → pattern →
   interface-foundations → recipe → gravity-foundations → библиотеки (routing / версии — в роутере).
   Имена компонентов и пропы сверяй с гайдами (`registry/guides/library-uikit.md` и др.) **перед**
   импортом — не подставляй из памяти других дизайн-систем.
2. **Проверь профиль сервиса.** Поищи в корне целевого проекта `service-profile.json` (или
   `.gravity/service-profile.json`):
   - **Есть** → это per-service слой «как принято у нас» (тема/бренд, плотность, раскладка,
     форм-политика, сабсет компонентов, архетипы, словарь). Примени его ручки поверх дефолтов по
     разделу **«Профиль сервиса (если передан)»** в `registry/AGENTS.md`: профиль уточняет открытые
     ручки, **не отменяет** корректность registry, омиссия = дефолт.
   - **Нет** → собирай по универсальным дефолтам (baseline).
3. **Собери** запрошенное как готовый к запуску React + TypeScript проект; версии пакетов — verbatim
   из `registry/registry.json` (`libraries[]` / `bundles[]`), route-only либы ставь целым bundle.
4. **Самопроверка (гейт) — после зелёного `tsc`/`build`.** Инструкция — `harness/HARNESS.md` (рядом со
   скиллом): собери с живыми сценариями (`VITE_SCENARIOS=1 npm run build`), подними `vite preview`,
   прогони `harness/run-gate.mjs` и разбери `verdict.json`. Находки чини **классами по гайдам registry**
   (не заплатками под чек), максимум 2 итерации на находку — остаток честно опиши пользователю. Нет
   Playwright — гейт сам деградирует в static-only (частичный вердикт; полный режим ставится по
   согласию пользователя, хромиум ~130 МБ). Предпросмотр в конце оставь работающим и дай URL
   (если пользователь не просил без фоновых процессов).

## Чего не делать

- Не выдумывать компоненты/пропы и не брать «похожие» из других библиотек — всё сверять с registry.
- Профиль **не переопределяет** корректность/идиому (имена, версии, app-shell, «FormRow вместо
  инлайн-`label=`»). При конфликте побеждает registry — пометь комментарием.
- Не подгонять код под гейт-чек и не редактировать файлы `harness/` — находки чинятся по гайдам;
  подозрение, что гейт ошибается, — опиши пользователю, не «чини» здоровый код.
- Static-only-вердикт не выдавать за полную проверку (browser-линии в нём пропущены).

## Структура бандла (читать on-demand)

- `registry/registry.json` — **роутер и entry-point** (workflow + libraries / версии + routing + patterns +
  recipes + foundations + scaffold). **Начни здесь.**
- `registry/guides/*.md` — вся проза: library-каталоги, recipes, patterns, foundations (факт живёт в одном guide).
- `registry/AGENTS.md` — тонкий companion: применение профиля сервиса + fallback при заблокированном фетче.
